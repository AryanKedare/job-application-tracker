// This file intentionally left as a redirect — see next.config.ts
// Vercel uses next.config.ts as the primary config.
// This file must exist but should be empty to avoid conflicts.

/** @type {import('next').NextConfig} */
const nextConfig = {}
export default nextConfig
